{
	"name": "cyan-plugin",
	"version": "1.0.0",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"watch:js": "npm run build:js -- --watch",
		"watch:css": "tailwindcss -i ./assets/css/tailwind.css -o ./assets/css/style.css --watch",
		"build:js": "node build.js",
		"build:css": "tailwindcss -i ./assets/css/tailwind.css -o ./assets/css/style.min.css --minify",
		"build": "npm run build:css && npm run build:js"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"description": "",
	"dependencies": {
		"autoprefixer": "^10.4.20",
		"postcss": "^8.4.47",
		"tailwindcss": "^3.4.13"
	},
	"devDependencies": {
		"esbuild": "^0.24.0"
	}
}
